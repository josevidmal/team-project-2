<section class="trip">
        <h2><a href="/trips/{{id}}">{{trip_name}}</a></h2>
        <h4>{{destination}}</h4>
        <h4>{{trip_date}}</h4>
        <img src="/images/{{trip_image}}" alt="{{trip_name}}">
</section>