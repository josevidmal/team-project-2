<section class="trip">
        <h2><a href="/trip/{{trip_id}}">{{trip_name}}</a></h2>
        <h4>Destination: {{destination}}</h4>
        <h4>Date: {{format_date trip_date}}</h4>
        <img src="/images/{{trip_image}}" alt="{{trip_name}}">
</section>